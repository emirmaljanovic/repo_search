# repo_search

## How to run
 - clone the repo
 - run `cd  repo_search`
 - run `npm install` or `yarn`
 - run `npm run start`
 - open app at `http://localhost:1234/`

## How to build
 - run `npm run build`